{
  "name": "simulator",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist/* && rm -rf cache && code ./ && parcel app/views/*.pug --no-hmr",
    "build": "rm -rf docs && parcel build app/views/*.pug --no-source-maps --out-dir docs --public-url '.'"
  },
  "keywords": [],
  "author": "proper-code",
  "license": "ISC",
  "devDependencies": {
    "figlet": "*",
    "parcel-bundler": "*",
    "pug": "^2.0.4",
    "stylus": "^0.54.7"
  },
  "dependencies": {}
}
